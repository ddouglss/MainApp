<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MainApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MainApp.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="MainApp">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:HomeView}"
        Route="MainPage" />

    <TabBar Route="Main">
        <ShellContent
            Title="Inicio"
            ContentTemplate="{DataTemplate views:MainView}"
            Route="MainPage"/>

        <ShellContent
            Title="Usuários"
            ContentTemplate="{DataTemplate views:UsuariosView}"
            Route="Usuarios"/>
    </TabBar>
</Shell>
